# Enable GitHub Pages

1. Repository **Settings → Pages**
2. **Build and deployment → Source:** GitHub Actions
3. Push to `main` runs `.github/workflows/pages.yml`
4. Site URL: `https://louisletcher.github.io/LouisLetcher/`

The portal source lives in `/docs` with Jekyll (`docs/_config.yml`).
